# Lessons

_Auto-generated by `graphify reflect` from 7 session memories in graphify-out/memory/. Deterministic; no LLM. Use for orientation — verify before relying, and revisit dead ends if the code has changed since._

## Summary

- 4 useful · 0 dead ends · 3 corrected · 0 unmarked

## Lessons

**Preferred sources** — corroborated by ≥2 useful results; start here.

- `InMemoryRepository` (2× useful)

**Tentative** — useful in fewer than 2 results; verify before relying.

- `makeTestApp()` (1× useful)
- `withTestApp()` (1× useful)
- `InMemoryCache` (1× useful)
- `requireAuth()` (1× useful)
- `verifyJwt()` (1× useful)
- `name` (1× useful)
- `Repository` (1× useful)

**Contested** — mixed signals; recency decides.

- `makeRouter()` — 1× useful, 1× dead end/corrected → recency leans **dead end** (latest 2026-06-30)

**Corrections** — do these differently.

- "Why does resolve() bridge i18n Resolution to eleven persistence/review communities (betweenness 0.179)?" → resolve() in migration tests is node:path.resolve, not the i18n resolver; the 13 INFERRED calls edges onto i18n_resolve_resolve are false. i18n resolve() degree is really 3 (resolve.ts contains, translate calls, resolve.totality.test imports).
- "Are the 32 INFERRED 'calls' edges involving t() correct?" → Component t(...) calls are the destructured return of useT(); they do not call the test-file local t in workspaceAuthedFetch.test.ts. The 32 INFERRED calls edges onto api_workspaceauthedfetch_test_t are false. useT() is the accurate i18n hub.
- "Do parse/get/query have the same node-collision pattern as resolve()/t()?" → parse/get/query are NOT collisions; only bare free-function names collide. .render() IS a third collision (RTL render calls collapsed onto App.tsx render). The 9 INFERRED calls edges onto App's .render() are false.
